"""Seeds used to characterize the oracle-label finding and a disjoint
holdout evaluated exactly once.
"""
from __future__ import annotations

TUNING_SEEDS = list(range(15))
HOLDOUT_SEEDS = list(range(1000, 1015))
